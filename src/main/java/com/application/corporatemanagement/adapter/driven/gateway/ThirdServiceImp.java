package com.application.corporatemanagement.adapter.driven.gateway;

public class ThirdServiceImp {

    // get Emp from orgmng context
    // note : the Emp is not the one from orgmng, but is the one from self context
//    public Emp findById(Long id) {
//        return null;
//    }
}
